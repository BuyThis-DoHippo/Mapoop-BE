spring.application.name=Mapoop

# MCP (Model Context Protocol) ??
mcp.enabled=true
mcp.server.url=http://localhost:3000
mcp.websocket.enabled=true
mcp.timeout=30000

# WebSocket ??
spring.websocket.enabled=true
